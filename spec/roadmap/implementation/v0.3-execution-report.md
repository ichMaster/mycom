# Phase v0.3 -- Execution Report

**Date:** 2026-08-03
**Branch:** main
**Label:** v0::version:0
**Target release:** 0.3.0
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 6 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | MC ID | Title | Phase | Status | Commit | Files | Tests |
|---|-------|-------|-------|--------|--------|-------|-------|
| 1 | MC-019 | Selection model core: Ins/Space, yellow rows, footer count, per-panel lifecycle | v0.3 | completed | 27ee7d6 | 7 | pass |
| 2 | MC-020 | Mask select/deselect dialogs | v0.3 | completed | 053fca3 | 8 | pass |
| 3 | MC-021 | Invert selection + select-all via mask | v0.3 | completed | 6c188b6 | 5 | pass |
| 4 | MC-022 | Ctrl+H global hidden-files toggle | v0.3 | completed | ba0ea19 | 4 | pass |
| 5 | MC-023 | State DB engine: SQLite (WAL), schema, migrations, corrupt-recreate | v0.3 | completed | 593729c | 2 | pass |
| 6 | MC-024 | Wire persistence into app lifecycle | v0.3 | completed | b2275f5 | 3 | pass |

Test suite grew from 156 (end of v0.2, post-release) to 218 passed across the six issues;
`ruff check` clean after every commit.

## Detailed Results

### MC-019: Selection model core
**Status:** completed · **Commit:** 27ee7d6
**Validation:** [x] pytest (173 passed) · [x] ruff · [x] py_compile · [x] contracts · [x] acceptance
Per-panel `_selected: set[str]`, Ins/Space toggle-and-advance, yellow Rich-markup rendering in all
three view modes, real footer count/size, survives sort/view changes, clears on navigation.
`get_selected_files`/`deselect`/`replace_selection` round out the v0.4 contract. Bonus: Ctrl+U now
swaps selections too (completes a v0.1 roadmap item deferred only for lack of a selection model).

### MC-020: Mask select/deselect dialogs
**Status:** completed · **Commit:** 053fca3
**Validation:** [x] pytest (186 passed) · [x] ruff · [x] py_compile · [x] contracts · [x] acceptance
`mycom/utils/masks.py::match_any`; reuses v0.2's `InputDialog` unchanged. Found and fixed a real
bug: `DialogKit`'s `key_enter`/`key_escape` didn't stop event bubbling, so dismissing a dialog with
Enter also reached `MyComApp.on_key`'s panel handling underneath — fixed by folding Enter/Esc into
`on_key` (which can call `event.stop()`) plus a defense-in-depth screen-stack guard in
`MyComApp.on_key`.

### MC-021: Invert selection + select-all via mask
**Status:** completed · **Commit:** 6c188b6
**Validation:** [x] pytest (193 passed) · [x] ruff · [x] py_compile · [x] contracts · [x] acceptance
`invert_selection()`; select-all needed no new code (MC-020's mask dialog with default `"*"`
already covers it).

### MC-022: Ctrl+H global hidden-files toggle
**Status:** completed · **Commit:** ba0ea19
**Validation:** [x] pytest (197 passed) · [x] ruff · [x] py_compile · [x] contracts · [x] acceptance
`set_show_hidden()` with cursor-preserve/fallback; app-level toggle updates both panels from one
keypress. Moved the config→`show_hidden` read from `__init__` to `compose()` to keep an existing
v0.1 test-override pattern working now that the value is live app state.

### MC-023: State DB engine
**Status:** completed · **Commit:** 593729c
**Validation:** [x] pytest (209 passed) · [x] ruff · [x] py_compile · [x] contracts · [x] acceptance
`mycom/state.py::StateDB` — WAL, versioned schema, migration runner, corrupt-file recreate, typed
accessors. Pure engine, no Textual dependency, nothing wired into the app yet.

### MC-024: Wire persistence into app lifecycle
**Status:** completed · **Commit:** b2275f5
**Validation:** [x] pytest (218 passed) · [x] ruff · [x] py_compile · [x] contracts · [x] acceptance
Startup restore (with vanished-path fallback to nearest ancestor then `$HOME`), debounced saves on
every state-affecting action, synchronous flush on quit. Found and fixed a real bug in
`_resolve_startup_path`: naive `is_dir()` walk-up never reaches the `$HOME` tier since `/` always
exists. `tests/conftest.py` gained a `state.db` isolation fixture (same pattern as the existing
log-file one).

## Next Steps

Phase v0.3 complete — awaiting review-and-fix-issues, docs update, and `/release-version 0.3.0`.
